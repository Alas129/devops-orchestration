image:
  repository: REPLACE_ECR_REGISTRY/usf-devops/notifier-svc
  tag: git-replaceme

migration:
  enabled: true
  image: REPLACE_ECR_REGISTRY/usf-devops/notifier-svc-migrate

ingress:
  enabled: true
  host: api.qa.example.com
  pathPrefix: /api/v1/notifier

serviceAccount:
  irsaRoleArn: REPLACE_QA_NOTIFIER_SVC_IRSA_ARN

externalSecret:
  enabled: false
