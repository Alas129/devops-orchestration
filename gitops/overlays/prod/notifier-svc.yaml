image:
  repository: 164856787183.dkr.ecr.us-east-1.amazonaws.com/usf-devops/notifier-svc
  tag: git-replaceme   # populated by promote-prod.yaml on stable release tag

migration:
  enabled: false   # see dev overlay

ingress:
  enabled: true
  host: api.calmloop.space
  pathPrefix: /api/v1/notifier

serviceAccount:
  # Populated by tools/populate-prod-from-tf.sh after `terraform apply` on envs/prod.
  irsaRoleArn: REPLACE_PROD_NOTIFIER_SVC_IRSA_ARN

externalSecret:
  enabled: false
