image:
  repository: REPLACE_ECR_REGISTRY/usf-devops/notifier-svc
  tag: git-replaceme

migration:
  enabled: false   # see dev overlay

ingress:
  enabled: true
  host: api.prod.example.com
  pathPrefix: /api/v1/notifier

serviceAccount:
  irsaRoleArn: REPLACE_PROD_NOTIFIER_SVC_IRSA_ARN

externalSecret:
  enabled: false
