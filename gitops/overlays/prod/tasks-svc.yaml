image:
  repository: REPLACE_ECR_REGISTRY/usf-devops/tasks-svc
  tag: git-replaceme

migration:
  enabled: false   # see dev overlay

ingress:
  enabled: true
  host: api.prod.example.com
  pathPrefix: /api/v1/tasks

serviceAccount:
  irsaRoleArn: REPLACE_PROD_TASKS_SVC_IRSA_ARN

externalSecret:
  enabled: true
  jwtSsmPath: /devops/auth-svc/jwt-secret
