image:
  repository: REPLACE_ECR_REGISTRY/usf-devops/notifier-svc
  tag: git-replaceme

migration:
  enabled: true
  image: REPLACE_ECR_REGISTRY/usf-devops/notifier-svc-migrate

ingress:
  enabled: true
  host: api.dev.example.com
  pathPrefix: /api/v1/notifier

serviceAccount:
  irsaRoleArn: REPLACE_DEV_NOTIFIER_SVC_IRSA_ARN

externalSecret:
  enabled: false
